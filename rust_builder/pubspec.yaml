name: rust_lib_kivixa
description: Native Rust library for Kivixa AI features
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.0.0'

dependencies:
  flutter:
    sdk: flutter
  ffi: ^2.0.0

flutter:
  plugin:
    platforms:
      ios:
        pluginClass: RustLibKivixaPlugin
        ffiPlugin: true
      linux:
        pluginClass: RustLibKivixaPlugin
        ffiPlugin: true
      macos:
        pluginClass: RustLibKivixaPlugin
        ffiPlugin: true