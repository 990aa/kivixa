{
  "name": "kivixa",
  "private": true,
  "scripts": {
    "bootstrap": "pnpm install",
    "build:all": "pnpm -r build",
    "test:all": "pnpm -r test"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
