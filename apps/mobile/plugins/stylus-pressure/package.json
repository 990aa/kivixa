{
  "name": "stylus-pressure",
  "version": "0.0.1",
  "description": "A custom Capacitor plugin for stylus pressure.",
  "main": "dist/plugin.js",
  "module": "dist/esm/index.js",
  "types": "dist/esm/index.d.ts",
  "scripts": {
    "build": "pnpm run clean && pnpm run compile",
    "clean": "rimraf dist",
    "compile": "tsc"
  },
  "peerDependencies": {
    "@capacitor/core": "^5.0.0"
  },
  "devDependencies": {
    "@capacitor/core": "^5.0.0",
    "rimraf": "^3.0.2",
    "typescript": "^5.0.0"
  }
}
