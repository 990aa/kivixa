class TextSelectionOps {
  // Store selection spans, create annotations, move text across docs
  void storeSelection(int pageId, Map<String, dynamic> span) {
    // Store selection
  }

  void createAnnotation(int pageId, Map<String, dynamic> coords, String type) {
    // Create annotation
  }

  void moveSelection(int fromDocId, int toDocId, int selectionId) {
    // Move selection
  }

  // Coordinate transforms and provenance logic to be implemented
}
