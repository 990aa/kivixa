/// Enum defining the available drawing tools for PDF annotation
enum DrawingTool {
  /// Standard pen tool for precise drawing
  pen,

  /// Highlighter tool with semi-transparent strokes
  highlighter,

  /// Eraser tool to remove annotations
  eraser,
}
